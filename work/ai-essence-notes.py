from pathlib import Path
import json
root=Path(__file__).resolve().parents[1]
p=root/'outputs/video-notes/4.AI-本质'
notes=[]
def add(title,body,visual=''):
 notes.append(dict(id=len(notes)+1,title=title,body=body.strip(),visual=visual.strip()))

add('从裸模型、模型服务走到 AI 应用',r'''
我是如何看待这些新概念的？又是如何学习它们的？我要把这个方法教给同学们。所以这节课为什么这么重要？你只要掌握好了这个方法，将来无论遇到什么样的概念，随便来，我保证几分钟就能搞定。有这么神奇吗？听一听就知道了。

好，各位同学，这节课的内容非常、非常重要。可以这么说，只要 AI 存在一天，这节课的内容就会发挥作用一天。

回顾一下，之前我们讲过裸模型和模型服务。接下来要讲 **AI Application，也就是 AI 应用这一层**。每一层实际上都是对下一层的封装：模型服务封装了裸模型的调用，AI Application 又封装了模型服务。它们就是这样的关系。在继续讲之前，有几点需要先说明。
''')

add('应用与模型服务的边界为什么会移动',r'''
第一，AI Application 和 Model Service 这两层的分界线没有那么清晰。裸模型与模型服务之间的分界比较清楚：裸模型接收一个 Token 列表，得到一个概率分布；模型服务通过之前讲过的自回归方式调用裸模型，同时进行前处理和后处理。

但是应用和模型服务之间，很多概念可能今天在这一层，明天就变到另一层。例如同学们现在使用的 Skills，最早是应用层玩出来的概念。后来模型服务商觉得这个概念不错，就把它融入模型服务层。这样开发 AI 应用的人就不用自己处理这部分 Skills 逻辑，因为模型服务已经做了。

来看课堂展示的 OpenAI 接口，其中就有 Skills。直接调用这个 API，就可以实现相应功能。如果开发应用时调用的是这样的接口，应用里便不必再写同样的 Skills 逻辑。这只是一个例子。之前的 MCP、Tools 也是类似的情况：有些模型服务商直接支持，应用开发者就不用自己处理那一部分功能逻辑。

这两层边界虽然不分明，但事情总要有人做，例如 Skills 与模型之间的交互逻辑。**如果你使用的服务商没有做，你就必须做；如果它已经做了，你就不需要重复做。**总归有一层要做。服务商多做一点，我就少做一点；它少做一点，我就必须多做一点，总归需要有人来做。

所以，这两层不太稳定。学到这里，同学们可以发现裸模型这一层非常稳定。虽然训练方式、性能、参数，以及注意力机制等内部做法不断更新，但它的调用方式很稳定。到了上面的模型服务和 AI 应用层，变化就多了。自媒体上每天大呼小叫的，主要就是这两层的变化。
''')

add('微调案例：训练时间之外，还有验证成本',r'''
这也符合技术栈中的一个规律：越底层越稳定，越上层越不稳定。说到这里，顺便谈谈为什么我认为现在很少有人去做微调了。之前说过，微调和训练都在生成裸模型需要调用的权重矩阵，也就是改动里面的参数。从成本上考虑，这件事可能划不来。

举个例子，假设原有权重矩阵基于 GPT-3.5。你觉得它的能力不够，公司又有大量内部知识没有经过训练。过去会怎么做？对模型进行微调，把原先权重矩阵里的某些参数通过二次训练改掉，生成一个基于 GPT-3.5、但修改过的权重矩阵。

这件事要做多久？我的说法是至少两三个月。微调本身的训练不一定花很多时间，主要麻烦在于验证训练结果是否符合预期。这个验证过程非常麻烦。两三个月还是往少了说，做得不顺，半年也有可能。

你好不容易花了半年微调出来，效果还不错，结果 GPT-4 出来了。在这个例子里，你发现 GPT-4 不用微调，只喂一些提示词就能解决问题：上下文窗口更大，识别更准确，幻觉更少，甚至直接使用的效果就超过之前微调的结果。那之前半年在干什么？花了那么多成本。我说的还不主要是算力成本，主要是人力成本，是大量验证的成本。

这些成本花过之后，大模型更新了，直接用新模型可能比原来的微调结果更好。反复这样折腾，有些公司就发现微调这件事不好做。因为 AI 还在不断发展，尚未成熟；**调用方式可以稳定，但训练出来的能力还在变化。**所以，在这样的条件下，我认为微调可能不划算。
''')

add('AI 应用层：应用开发与工具使用',r'''
因此，我的判断是：除了少部分极端场景，现在主要关注的不是微调，而是模型服务和 AI 应用这两层。模型服务通常是别人提供的接口，我们能发挥的主要是应用层。

应用层有两个方面：一个是**应用开发**，一个是**工具使用**。所谓 AI 应用开发，就是调用模型服务商提供的接口，做出一个具体、有价值的 AI 工具。后面要讲的 AI 效能工具，主要是 AI 编程工具。请问这些编程工具属于哪一层？是裸模型层吗？不是。是模型服务层吗？也不是。它们属于 AI 应用层。

应用开发，就是开发一个应用，调用这些接口，完成有价值的功能。除了开发，还包括如何使用别人已经开发出来的工具。这两个方面很好理解。

展开说，做应用开发首先要知道各种概念，例如 Tools、MCP、RAG、Skills 等。很多 AI 概念都和应用层有关。当然，刚才说过，两层之间的界限并不绝对，有些概念已经被模型服务商吸收，由服务商直接提供。但即使服务商提供了，你也要知道这个概念，否则不知道怎么使用。

第二，还需要了解开发框架，例如 **LangChain、LangGraph、DeepAgent** 等常见框架。如果要做应用开发，这些是需要了解的。课堂接下来展示架构课中的相关内容：除了架构项目，也包含 AI 应用开发、AI 相关概念和框架。
''')

add('课程体系说明与工具使用的定位',r'''
这里展示的课程内容，先是 AI 相关概念，然后是 LangChain、LangGraph，后面还有 AI 项目。比如同学们使用的 AI 编程工具，课程会实践一遍如何做出这样的工具，之后还会加入其他 AI 应用开发项目。

架构课的定位很明确：如果已经工作两三年，薪资遇到瓶颈，希望通过课程打开进一步发展的空间，可以了解这类内容。高薪课是从零基础到高级业务开发，架构课则是从高级业务开发到架构师，AI 应用开发内容放在架构课里。

除了体系化课程，还有一些福利课程，讲付费课里的核心知识点，送给大家，希望在较短时间内解决核心问题。有些同学仅通过这些福利课程，也实现了薪资提升。如果想了解，可以在账号主页点击头像，进入主页后根据提示联系。

回到主题。AI 应用层除了应用开发，还有这门课最后要讲的 AI 效能工具，也就是工具使用。这部分不涉及自己开发，而是**如何利用工具提高工作效率**。这里同样涉及概念，而且对概念的认识和应用开发那一侧没有区别。如果不认识这些概念，工具也很难使用好。
''')

add('先理解概念，再学习具体编程工具',r'''
然后才是具体工具介绍，比如 Claude Code、Codex、OpenCode 等。理解概念之后，稍微点拨一下，就能明白工具怎么用。有些同学使用工具时感到头疼，不知道该怎么用，往往就是概念不清。

这个体系就讲清楚了：前面的技术认知讲完，后面主要讲 AI 编程工具，因为这门课面向程序员。例如 AI 短剧，虽然也属于工具使用层面，却不在这门课的范围内。

如果后面有新奇的工具，也可以介绍，例如课堂提到的 OpenClaw。不过很多新工具的生命周期很短，有些可能一周都撑不过，有些能撑几个月，很快又销声匿迹。我认为不少工具是在玩概念。当然，同学们有兴趣，介绍一下也没问题。
''')

add('理解新概念，只盯住输入与输出',r'''
那么这节课是要逐个讲这些概念吗？不是。这节课要讲的比概念更重要：**如何去理解概念**。我给的不是鱼，不是钓鱼竿，也不是钓鱼装备，而是钓鱼的方法。

AI 发展太快，自媒体每天都有新东西，似乎每天都要颠覆一下全人类。新概念层出不穷，可能这边刚讲完，那边某些概念就失效了，又出现新的。面对这种情况，同学们该如何自处？为什么你们天天慌，我却不慌？因为在我眼里，严格说，它们的本质没有那么大区别，有不少是新瓶装旧酒。

我是如何看待和学习新概念的？我要把这个方法教给大家。这就是本节课重要的原因：掌握这个方法以后，将来遇到新概念，就能很快理解。有这么神奇吗？接下来就看一看。

先抛出一个论点：在裸模型基础上做的事情，核心只有两个问题。

1. **输入什么给模型？**
2. **如何处理模型的输出？**

无论是模型服务还是 AI 应用，玩的都是这两件事。两层边界不绝对，你多做一点，我就少做一点。第一个问题：我给模型什么？第二个问题：它给我一个东西，我怎么办？没有别的核心问题了。

这是由模型的本质决定的：给它一个输入，经过一串数学运算，得到一个输出。正因为模型是这样，我们在它上面做事情，就围绕这两个问题展开。
''')

add('用前处理、后处理检查这两个问题',r'''
由于模型的本质如此，我们能处理的就是：我给它什么，以及拿到它的结果后再做什么。来看之前模型服务的图。前处理做什么？回忆一下，有 Token 化，还会喂一些系统提示词。做来做去，目的都是准备好要交给模型的东西。因此，前处理对应的是“输入什么给模型”。

后处理是什么意思？模型给了结果之后，还要做后续工作。例如之前讲过的反 Token 化，以及合规审查，都属于后处理。

对模型服务如此，对 AI 应用也是一样。AI 应用要考虑给模型发什么消息。应用可以把整个模型服务看成一个模型：虽然内部有处理，但对应用而言，这是一个黑盒，不必关心全部内部步骤。知道给它一个消息，它会回一个消息，然后考虑该传什么即可。

有人会问：“这个消息不是用户填的吗？”是用户填的，但**用户的消息经过 AI 应用后，应用可能会增加消息**。一会儿就演示给大家看。AI 应用会加入额外提示词，一起传给模型；模型吐出结果后，应用又会做后续处理。
''')

add('HTTP 类比：看消息、格式与后续动作',r'''
所以，无论 MCP、Tools 还是 Skills，不管学过还是没学过，观察时都可以沿着这个思路：应用给模型输入什么，又如何处理模型输出。

这是不是有点像 HTTP 的请求和响应？我给你发什么，你给我回什么。例如协商缓存，本质上也是约定请求里发什么、采用什么格式，响应时返回什么。大文件上传也是如此：发什么格式的消息、发几次，对方又响应什么。OAuth 或单点登录同样在规定请求和响应。现在对模型，也可以用类似方式理解。

为什么这两层在我眼里比较简单？无非天天研究这些东西。新概念再多，核心也就这些。如果自媒体直接告诉你这么简单，那就不够劲爆，也没法天天“颠覆全人类”了。但实际上，同学们遇到这两层里的新概念，只要做一件事，就能把它搞清楚：

**看它给模型传了什么，模型吐出了什么，然后研究它接下来怎样处理。**

比如 Skills，听起来很神秘，同学们天天问。听完这节课，你会发现它很简单。只要看传给模型的东西、模型的输出，以及后续处理即可。我只是以 Skills 为例，将来遇到其他概念，也可以用类似方法分析。这节课要教的是理解概念的方法。
''')

add('搭一层代理，观察请求和响应',r'''
怎么观察？这里需要一个代理服务器。假设用户使用 Claude Code，也可以换成其他编程工具。正常情况下，编程工具通过 HTTP 请求调用模型服务，模型服务把结果传回 Claude Code，Claude Code 经过一系列处理后，再把结果交给用户。

现在要分析的是：Claude Code 支持 Skills，它到底如何围绕 Skills 与模型服务传递消息？有两种方式。一种是抓包。HTTPS 能不能抓？也可以，但涉及额外工具，一些同学不熟悉。这里用更直观的办法：加一层代理服务器。

稍微配置 Claude Code，让它向模型发消息时先发给我们的代理。代理转发给模型服务；模型回复后，再经过代理反馈给 Claude Code。因为这个代理是我们自己写的，就可以记录日志，看到发出去和返回来的内容。

链路变成：**用户 ↔ Claude Code ↔ 代理服务器 ↔ 模型服务**。

先把这个工具准备出来。不熟悉 Node.js，也可以把要求告诉 AI，让它帮你写，或者使用课堂准备的源码。这里的项目已经写好了，后续可以提供给同学们。实现很简单，我也是让 AI 写的，主要就是一个 JS 文件：启动服务器，接收请求，再转发。接下来先把它运行起来。
''')

add('配置 .env，用 curl 验证代理日志',r'''
先运行项目，课堂中使用 `npm run dev`，服务器监听 3000 端口。这里先停止，检查一下环境变量配置。

拿到项目后，需要把 `.env.example` 复制一份，或者改名为 `.env`。`.env` 没有加入 Git 管理，因此要自己准备。这里至少有几类配置：日志级别、代理服务器的端口，以及请求转发的目标地址。日志级别暂时不展开；端口决定服务器在哪里监听，目标地址决定收到请求后转发到哪里。

先随便选一个目标测试，比如百度。设置后，把请求发到本地代理的 3000 端口。课堂用 `curl` 发起请求。拿到了响应，响应具体是什么暂时不重要，重点是观察代理有没有收到请求。

看代理日志：它收到了请求，并把请求转发到指定地址；**请求头、请求体、响应头、响应体**都会记录下来，便于观察。这个代理主要就是做这件事，没有别的复杂功能。

第一步，代理服务器准备好了。下一步，要让 AI 编程工具调用模型服务时，实际走这个代理。
''')

add('让 Claude Code 通过代理访问模型',r'''
要让 AI 编程工具请求代理服务器，否则它仍然直接访问模型服务，就看不到中间的消息。我见过的 AI 开发工具通常都支持配置请求地址。具体用哪个工具、怎样配置，可以自行查该工具的设置，这里不逐个讲，后面还会专门介绍。

课堂以 Claude Code 为例，通过 **CC Switch** 配置。有些同学知道这个工具，有些不知道，暂时不影响理解。这里启用一个名为 **Kimi-Proxy** 的配置，希望 Claude Code 调用本地代理，再由代理转发到 Kimi。我使用的不是 Claude 官方模型，而是 Kimi 模型。究竟哪个模型好，以后再讲；这里选什么模型不影响观察方法。

编辑配置，把请求地址改成我们的代理地址，其他配置不变，然后启用。后续 Claude Code 的请求就会走代理。也可以看到，这个工具实际是在生成相应配置。课堂展示配置时，讲者发现密钥出现在画面中，顺口提到已经暴露；理解这个流程不需要密钥的具体值。

启用后，随便找个工程，进入 Claude Code。现在这条链路就通了。不过还要改代理服务器的配置：目标地址不能继续是百度，要改为 Kimi 的模型服务地址，然后重新启动代理。

再进入 Claude Code，看它到底发送了什么。进入之后，代理就生成了一条日志。
''')

add('还没输入问题，应用已经发送了工具描述',r'''
看这条日志，Claude Code 已经向模型发出了一条消息。请求地址和请求头在这里，请求体是 JSON 格式。

先问大家：我们有没有在 Claude Code 里输入任何消息？没有。说明什么？用户无论有没有填写内容，都会经过 AI Application 的封装。应用可能增加额外消息；甚至用户还没输入，它就已经向模型发送一段消息了。

为了看清楚，把请求体复制出来，建立一个 JSON 文件保存。简单看一下：首先有一条用户角色的消息，用于测试模型是否连通；然后还传了 `tools`。Tools 的概念以后会讲。

在工具列表中，有一个叫 **Skill** 的工具。它在描述什么？它告诉模型什么叫技能、技能的含义，并约定：如果将来要使用一个技能，应返回什么内容。至少需要技能名称，还包括参数怎样传递。

这些消息传过去，模型就知道技能是什么，以及如果要使用技能，应当怎样回复。我们此时还没有做什么，应用就已经把这些内容传过去了。
''')

add('“你是谁”的答案，来自应用添加的提示词',r'''
接下来输入一句：“你是谁？”它回答自己是 Claude Code，是一个 CLI 工具。这个回答是谁生成的？当然是模型。但明明用的是 Kimi 模型，为什么会这样回答？看一下请求就明白了。

这里发了好几条请求，随便取一条，把请求体复制到 JSON 文件里，方便查看。搜索“你是谁”，能找到用户提出的问题。但是模型只收到了这句话吗？并不是，前面还有很多消息。

例如，这段是工程中的 `CLAUDE.md` 内容，里面写着项目规范。课堂提到这个文件会随着消息带给模型，日志里也能看到它确实被带上了。除此以外，还有 `system-reminder`，也就是 Claude Code 工具额外加入的系统提醒。

提醒里告诉模型有哪些技能可用，例如 `docker-compose`、`skill-creator`、`web-design-guidelines`。每个技能后面都有描述，告诉模型这个技能做什么、什么时候应该使用它。至于怎样调用，前面的工具说明已经约定了回复消息的格式。

你看，技能的这些信息是通过提示词传过去的：告诉模型有哪些技能、何时使用，以及怎样返回调用消息。

再看系统提示词，还告诉模型“你是谁”，把它描述为一个可交互的 CLI 工具。所以刚才问“你是谁”，模型就是从这里找到答案的。**这来自 Claude Code 给模型喂的提示词。**再回头看 Skills 等功能在干什么：就是在控制输入给模型的东西。
''')

add('触发一次技能，再去查真实请求',r'''
接下来看第二个问题：应用如何处理模型输出？再做一次实验，看看技能是怎么调用的。技能描述里说明了什么样的请求会触发它，这里就用相应语句发消息。

课堂输入的是 `review UI/UX`。可以看到，工具成功加载了技能，并开始询问用户要使用哪一种方案。它应当先读过技能文档，才知道要这样询问。这里先不做选择，转而观察请求是怎样发出去的。

在日志中定位用户请求，可以看到 `review UI/UX` 这段文本。先问大家：模型现在知不知道有哪些技能？知道，之前的消息已经告诉它了。它也知道什么时候应该使用技能。因此，这条用户消息进来之后，模型就可以判断要不要使用相应技能。

接下来找模型回复。技能名里有 `web-design`，因此在日志中搜索 `design`，定位到响应部分，看看模型到底回了什么。
''')

add('SSE 流里的自然语言与工具调用 JSON',r'''
模型通过 HTTP 返回响应。这里的格式见过吗？叫 **SSE，Server-Sent Events**，用于流式传输，每次传一段。

先看到一段自然语言，大意是“我将使用 Web Design Guidelines”。它对应工具界面里显示的那句话。这只是回复文本。真正调用技能，是否要满足某种约定格式？是的，最开始已经告诉模型：要使用技能，就返回规定的 JSON 内容。

继续往后看响应。前面是自然语言，后面开始 `tool_use`，表示使用工具。使用什么工具？技能工具。前面的工具列表已经告诉模型，有一个工具叫 Skill，使用时要传相应的 JSON。课堂中先前的一份日志已被删除，所以这里根据当前响应继续观察。

因为采用流式传输，JSON 也被分成了多段。能看到大括号、相应字段，再往后能找到 `web-design-guidelines` 这个技能名。把这些片段拼接起来，就得到了工具调用参数。

这样 Claude Code 就能拿到模型的响应，知道模型要调用哪个技能。接下来把整个流程串起来看。
''')

add('第一轮：封装输入，模型决定是否调用技能',r'''
用户先输入，消息发给 Claude Code。Claude Code 收到之后，加入一大堆系统提示词，再发给模型。因此模型收到的，既有用户输入，也有各种提示词。

提示词中包含了技能信息：有哪些技能，以及使用技能时需要怎样回复。这些内容一起传给模型。

模型此时根据拿到的输入进行判断：要不要使用技能？如果不需要，例如一开始问“你是谁”，和技能没关系，那正常输出即可。如果需要使用技能，就按约定返回 JSON。

这个 JSON 告诉 Claude Code：我要进行技能调用，我要使用哪个技能。它回到 Claude Code，当前例子里指定的就是 `web-design-guidelines`。

那么 Claude Code 拿到响应之后，如何处理？它不会把这一段调用消息直接当作最终答案发给用户，而是继续执行下一步。
''')

add('第二轮：读取技能全文，继续调用模型',r'''
下一步，Claude Code 读取技能文档。它能不能读？当然能，它本身就是一个本地程序。为了确认，打开这个技能文件，随便复制文档里的一句话，再到请求日志里搜索。

现在可以看到，Claude Code 把技能全文读出来了。一开始有没有把全文给模型？没有。一开始只是告诉模型有哪些技能；等真正需要使用时，才把技能完整文档给它。

日志里能找到文档的全部内容。读文件这件事本身不复杂：把文件内容读出来，再交给 AI，让模型继续处理。也就是说，Claude Code 把完整技能文档传给模型，模型后续处理后，结果再回到 Claude Code，最后传给用户。因此用户看到刚才的询问提示。

再捋一遍，哪些步骤是在给模型输入？用户输入加提示词，属于输入。模型返回工具调用，是输出。Claude Code 对输出做后续处理，读取完整文档，又调用一次模型，这又形成输入。模型再次输出，Claude Code 再交给用户。**这是一个完整的闭环。**

通过代理服务器，可以清楚看到：应用给模型传了什么，模型返回什么，应用怎样继续处理。再次重申，我现在不是专门讲 Skills；后面的 AI 效能工具课程还会讲它。这里只是通过这个例子演示分析方法。
''')

add('把观察方法迁移到下一个新概念',r'''
以后遇到各种新概念，怎么知道它到底是什么？并没有那么神奇。例如课堂提到 OpenClaw，我甚至猜想有些同学看到这段课时，它可能已经不流行了。不过它消不消失不重要，重要的是遇到新概念时，研究这两个问题：**到底输入什么给模型，又得到什么输出，应用如何继续处理。**

模型的这种输入输出方式，几年下来没有变。其他花样主要是这上面两层做出来的。AI 技术认知这一部分讲到这里，有同学可能会说：“你只解释了几个，还有好多概念没解释。”如果还需要我把每个概念都单独拆开讲，那就没有抓住这节课的重点。

这些概念后面要不要解释？要，会在 AI 效能工具部分解释。有了现在的认识，后面再说一个概念，不用每次都展示具体传输内容，我只需告诉你它给模型传什么、模型返回什么、接下来怎样操作，就能明白。后面还会比较各种概念。

所以，学任何东西要抓住核心。如果过分陷入细枝末节，就容易掉进知识的海洋，出不来了。
''')

add('抓住核心与后续课程安排',r'''
就像前端最火的那些年，也是各种概念层出不穷。我们的学员在意吗？并不特别在意。先看它到底在干什么，能玩出什么，本质上有很多仍是新瓶装旧酒，而且新东西未必能活很久。还是那句话：抓住核心。

AI 技术认知部分到此结束。下节课开始讲 AI 相关工具。后续内容不再全部免费发布，会放在付费课程里；如果全部放出来，付费学员也会有意见。

具体放在哪里？放在高薪课。高薪课和架构课都有 AI 内容，但层次不同：**架构课主要关注如何开发 AI 应用，高薪课主要关注如何使用 AI 应用。**使用工具属于业务开发；开发效能工具来提高效率、降低整个团队成本，则属于架构层面的工作。

开发 AI 应用的内容，架构课里已经有了；AI 效能工具的使用内容，放在高薪课。具体需要哪一类，可以再了解。联系的方式仍然是在账号主页点击头像，进入主页后，根据提示了解和领取课程。
''')

if __name__=='__main__':
 (p/'work/notes-data.json').write_text(json.dumps(notes,ensure_ascii=False,indent=2),encoding='utf-8')
 print('Prepared',len(notes),'full chapter drafts')
