## 标题
搭一层代理，观察请求和响应

## Light-plus
将来同学们遇到其他概念都可以用类似的方法来分析。所以这节课非常重要，我来教你如何来去理解一个概念。好，怎么做呢？嗯，我们这里呢需要一个代理服务器，看看，现在是用户，用户呢我们假设去使用一个随便吧，比如说 Claude Code 吧，啊当然也可以是别的编程工具，无所谓的。好，那么编程工具这边是不是应该正常的情况下应该是发给一个模型服务商，对不对？他去调这个模型服务，通过一个HTTP请求去调这个模型服务，然后去拿到结果，是这意思吧？

那么模型服务呢，传递结果给Claude Code，那么Claude Code呢，在经过一系列处理过后，把这个结果呢传递给用户，是这意思吧？那么现在呢，你要分析啥？分析这个Claude Code，它不是支持Skill吗？对不对？它是支持Skill的。我现在要分析的是Claude Code，它是把这个Skill是如何跟模型服务之间进行消息传输的。那我这如何来分析呢？，两种方式，一种是抓包，，但是它是HTTPS协议，抓包能不能抓？也可以抓。但是呢，涉及到一些工具，可能有些同学对那些工具都听懵了。

那我这里简单一点，我在这里呢去加一层代理服务器，那就变成了这个逻辑了。我就稍微配置一下Claude Code，让他向模型发消息的时候呢，不要直接发给模型服务，发给我们的代理服务器。我们代理服务器呢进行转发，把它发给模型服务。好，模型服务回复消息过后呢，经过我们的代理服务器，又反馈给这个Claude Code。那么由于这个代理服务器是我们自己写的，我们就可以做一些日志记录，我们就可以看得到他发的消息发的是什么。懂了，意思吧？好，我们先做这么一个工具出来。那么代理服务器呢，同学们如果说不熟悉Node.js的话

你给它提要求，让他帮你写也行，或者是在我这个源码里边直接去下载就行了。好，我这里呢已经写好了哈，我看一下我搁哪儿呢？哦，在这儿，proxy-server。后续的话，我可以把这个东西提供出来，同学们直接拿去用就行了。我这里写的非常简单哈，我也是让AI写的，就一个JS没了，就这个东西。嗯，做了是啥事儿呢？就是启动一个服务器，你给我发个请求，然后我进行转发。

## Visual explainer
便签中的链路是“用户 ↔ Claude Code（skill）↔ 代理服务器 ↔ 模型服务”。后续代码工程名为 proxy-server，源码入口为 index.js。
